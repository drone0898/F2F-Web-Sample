"use client";

/**
 * Game Page
 *
 * Main game interface page.
 * Renders the GameTerminal component with full terminal experience.
 */

import { GameTerminal } from "@/components/game/GameTerminal";

export default function GamePage() {
  return <GameTerminal />;
}
